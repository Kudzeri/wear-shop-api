
# API Documentation

## Установка проекта

1. **Клонирование репозитория**
   ```sh
   git clone <your-repository-url>
   cd <your-project-folder>
   ```

2. **Установка зависимостей**
   ```sh
   composer install
   npm install
   ```

3. **Настройка окружения**
   ```sh
   cp .env.example .env
   ```
    - Настроить соединение с базой данных в `.env` (DB_HOST, DB_DATABASE, DB_USERNAME, DB_PASSWORD)
    - Настроить хранилище (FILESYSTEM_DISK=public)

4. **Генерация ключа приложения**
   ```sh
   php artisan key:generate
   ```

5. **Миграции и наполнение базы данных**
   ```sh
   php artisan migrate --seed
   ```

## Запуск проекта

```sh
php artisan serve
```

## Генерация документации Swagger

1. **Установить пакет L5 Swagger (если не установлен)**
   ```sh
   composer require "darkaonline/l5-swagger"
   ```

2. **Сгенерировать документацию**
   ```sh
   php artisan l5-swagger:generate
   ```

3. **Открыть Swagger UI**
    - Перейти по адресу: `http://127.0.0.1:8000/api/documentation`

## Админка Filament

Для управления товарами, категориями, пользователями и их компонентами через панель администратора Filament выполните следующие шаги:

### Установка и настройка Filament

1. **Установить пакет Filament**
   ```sh
   composer require filament/filament
   ``
2. **Запуск админки**
   Для доступа к админке перейдите по адресу:
   ```
   http://127.0.0.1:8000/admin
   ```
   Введите ваши учетные данные для входа.

### Страницы и действия в админке:
- **Товары**: создание, редактирование, удаление товаров.
- **Категории**: управление категориями и подкатегориями товаров.
- **Пользователи**: управление пользователями, их данными и правами доступа.

## API Маршруты

## 🔐 **Аутентификация**
| Метод  | URL | Описание | Аутентификация |
|--------|-----|----------|----------------|
| `POST` | `/api/register` | Регистрация пользователя | ❌ |
| `POST` | `/api/login` | Авторизация пользователя | ❌ |
| `POST` | `/api/logout` | Выход | ✅ (Bearer) |
| `GET` | `/api/profile` | Получить профиль пользователя | ✅ (Bearer) |

---

## 🛍 **Товары (Products)**
| Метод  | URL | Описание | Аутентификация |
|--------|-----|----------|----------------|
| `GET` | `/api/products` | Получить список всех товаров | ❌ |
| `POST` | `/api/products` | Создать новый товар | ❌ |
| `GET` | `/api/products/{id}` | Получить товар по ID | ❌ |
| `PUT` | `/api/products/{id}` | Обновить товар | ❌ |
| `DELETE` | `/api/products/{id}` | Удалить товар | ❌ |
| `GET` | `/api/products/size/{size_slug}` | Получить товары по размеру | ❌ |

---

## 📂 **Категории (Categories)**
| Метод  | URL | Описание | Аутентификация |
|--------|-----|----------|----------------|
| `GET` | `/api/categories` | Получить все категории | ❌ |
| `GET` | `/api/categories/{slug}/parent` | Получить родительскую категорию | ❌ |
| `GET` | `/api/categories/{slug}/children` | Получить дочерние категории | ❌ |

---

## 🎨 **Цвета (Colors)**
| Метод  | URL | Описание | Аутентификация |
|--------|-----|----------|----------------|
| `GET` | `/api/colors` | Получить все цвета | ❌ |
| `GET` | `/api/colors/{id}/products` | Получить товары по цвету | ❌ |

---

## 📍 **Адреса (Addresses)**
**(Требуется аутентификация)**
| Метод  | URL | Описание | Аутентификация |
|--------|-----|----------|----------------|
| `GET` | `/api/addresses` | Получить список адресов пользователя | ✅ (Bearer) |
| `GET` | `/api/addresses/primary` | Получить основной адрес пользователя | ✅ (Bearer) |
| `POST` | `/api/addresses` | Добавить новый адрес | ✅ (Bearer) |
| `PUT` | `/api/addresses/{address}` | Обновить адрес | ✅ (Bearer) |
| `DELETE` | `/api/addresses/{address}` | Удалить адрес | ✅ (Bearer) |

---

## 📏 **Размеры (Sizes)**
| Метод  | URL | Описание | Аутентификация |
|--------|-----|----------|------------|
| `GET` | `/api/sizes` | Получить все размеры | ✅ (Bearer, Role: Admin) |
| `POST` | `/api/sizes` | Добавить новый размер | ✅ (Bearer, Role: Admin) |
| `GET` | `/api/sizes/{slug}/products` | Получить товары по размеру | ✅ (Bearer, Role: Admin) |
| `DELETE` | `/api/sizes/{id}` | Удалить размер | ✅ (Bearer, Role: Admin) |

---

## 👤 **Пользователи (Users)**
| Метод  | URL | Описание | Аутентификация |
|--------|-----|----------|-----------|
| `GET` | `/api/users` | Получить всех пользователей | ✅ (Bearer, Role: Admin) |
| `GET` | `/api/users/{id}` | Получить информацию о пользователе | ✅ (Bearer, Role: Admin) |
| `POST` | `/api/users` | Создать нового пользователя | ✅ (Bearer, Role: Admin) |
| `PUT` | `/api/users/{id}` | Обновить данные пользователя | ✅ (Bearer, Role: Admin) |
| `DELETE` | `/api/users/{id}` | Удалить пользователя | ✅ (Bearer, Role: Admin) |

---

## 📦 **Заказы (Orders)**
### **Для пользователей** (Требуется аутентификация)
| Метод  | URL | Описание | Аутентификация |
|--------|-----|----------|----------------|
| `POST` | `/api/orders` | Создать заказ | ✅ (Bearer) |
| `POST` | `/api/orders/{orderId}/pay` | Оплатить заказ | ✅ (Bearer) |
| `GET` | `/api/orders/{id}` | Получить данные заказа | ✅ (Bearer) |
| `GET` | `/api/orders` | Получить список заказов | ✅ (Bearer) |
| `POST` | `/api/orders/{order}/complete` | Завершить заказ | ✅ (Bearer) |
| `PUT` | `/api/orders/{id}/status` | Обновить статус заказа | ✅ (Bearer) |

### **Для администратора** (Требуется роль `admin`)
| Метод  | URL | Описание | Аутентификация |
|--------|-----|----------|----------------|
| `GET` | `/api/admin/orders` | Получить все заказы | ✅ (Bearer, Role: Admin) |
| `GET` | `/api/admin/orders/{id}` | Получить данные заказа | ✅ (Bearer, Role: Admin) |
| `POST` | `/api/admin/orders` | Создать заказ | ✅ (Bearer, Role: Admin) |
| `PUT` | `/api/admin/orders/{id}` | Обновить заказ | ✅ (Bearer, Role: Admin) |
| `DELETE` | `/api/admin/orders/{id}` | Удалить заказ | ✅ (Bearer, Role: Admin) |

---

## ❤️ **Избранное (Wishlist)**
**(Требуется аутентификация)**
| Метод  | URL | Описание | Аутентификация |
|--------|-----|----------|----------------|
| `GET` | `/api/wishlist` | Получить список избранных товаров | ✅ (Bearer) |
| `POST` | `/api/wishlist/{product_id}` | Добавить товар в избранное | ✅ (Bearer) |
| `DELETE` | `/api/wishlist/{product_id}` | Удалить товар из избранного | ✅ (Bearer) |

---

## 🎖 **Программа лояльности (Loyalty)**
**(Требуется аутентификация)**
| Метод  | URL | Описание | Аутентификация |
|--------|-----|----------|----------------|
| `POST` | `/api/loyalty/use-points` | Использовать баллы | ✅ (Bearer) |
| `GET` | `/api/loyalty/points` | Получить баланс баллов | ✅ (Bearer) |
| `GET` | `/api/loyalty/level` | Получить уровень лояльности | ✅ (Bearer) |

---

## 💳 **Платежи (Payments)**
**(Требуется аутентификация)**
| Метод  | URL | Описание | Аутентификация |
|--------|-----|----------|----------------|
| `POST` | `/api/pay` | Оплатить заказ | ✅ (Bearer) |
| `POST` | `/api/webhook` | Вебхук платежной системы | ✅ (Bearer) |

**Дополнительно:**
| Метод  | URL | Описание |
|--------|-----|----------|
| `GET` | `/api/order/payment/success/{orderId}` | Подтверждение успешного платежа |
| `POST` | `/api/yookassa/webhook` | Вебхук от ЮКассы |

---

## 🔥 **Что изменилось?**
✅ Добавлены **платежи**, **лояльность**, **избранное** и **заказы**
## Дополнительно

- **Настроить доступ к файлам (storage)**
  ```sh
  php artisan storage:link
  ```

---
